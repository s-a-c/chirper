/** @format */

import './bootstrap';

// Initialize application
// This ensures the module has executable code for test coverage
export const initializeApp = () => {
    // App initialization complete
    // Bootstrap has already been imported and executed
};
